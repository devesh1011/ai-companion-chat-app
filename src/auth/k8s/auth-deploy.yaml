apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-companion-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-companion-auth
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
  template:
    metadata:
      labels:
        app: ai-companion-auth
    spec:
      containers:
      - name: ai-companion-auth
        image: deveshk237572/ai-companion-auth:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - containerPort: 8001
        envFrom:
        - configMapRef:
            name: ai-companion-auth-configmap
        - secretRef:
            name: ai-companion-auth-secret